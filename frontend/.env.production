# GitHub Pages production build — standalone SOC simulation mode
# Leaves VITE_API_URL empty so the app runs self-contained without needing Render.
VITE_API_URL=
VITE_BASE_PATH=/unified-threat-platform/
